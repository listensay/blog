# AI 润色的配置。复制成 .env.local 后填上自己的值。
#
#   cp .env.example .env.local
#
# 用 .env.local 而不是 .env：`.gitignore` 里的 `*.local` 已经把它排除了，
# 密钥不会进 git。改完要**重启 dev server**，Vite 不会热加载 env 文件。
#
# 这些变量只在 Node 侧读（server/ai.ts），不会打进浏览器里的代码。

# 必填。没有它后台的 AI 按钮就是禁用状态，其余功能照常。
ADMIN_AI_API_KEY=

# OpenAI 兼容的接口地址，要带 /v1。不填就是 OpenAI 官方。
# DeepSeek        https://api.deepseek.com/v1
# 智谱 GLM        https://open.bigmodel.cn/api/paas/v4
# 月之暗面 Kimi   https://api.moonshot.cn/v1
# 阿里通义        https://dashscope.aliyuncs.com/compatible-mode/v1
# 中转站          照它文档给的地址填
ADMIN_AI_BASE_URL=

# 模型名。不填是 gpt-4o-mini。
# 中文润色可以试 deepseek-chat、glm-4-plus、kimi-latest、qwen-plus
ADMIN_AI_MODEL=

# 下面两个一般不用管。

# 单次请求超时（毫秒），默认 300000（5 分钟）。
# 推理模型很慢：实测修一篇 2100 字文章的格式要 2 分钟、大部分时间在「思考」。
# 文章很长又老是超时就往上调。
ADMIN_AI_TIMEOUT_MS=

# 输出长度上限（token）。默认**不传这个参数**，用供应商自己的默认值。
# 只有在弹窗里看到「结果被截断了」时才需要设它。
ADMIN_AI_MAX_TOKENS=
